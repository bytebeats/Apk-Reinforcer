<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.bytebeats.apk.reinforcer.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="me.bytebeats.apk.reinforcer.ProxyApplication"
        android:theme="@style/Theme.ApkReinforcer">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="app_name"
            android:value="me.bytebeats.apk.reinforcer.app.MyApplication" />
        <meta-data
            android:name="app_version"
            android:value="dexDir_1.0" />
    </application>

</manifest>